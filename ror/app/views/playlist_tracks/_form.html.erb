<%= form_for(playlist_track) do |f| %>
  <% if playlist_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist_track.errors.count, "error") %> prohibited this playlist_track from being saved:</h2>

      <ul>
      <% playlist_track.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
